import 'platform_io_interface.dart';

PlatformFile createPlatformFile(String path) => throw UnimplementedError();
PlatformDirectory createPlatformDirectory(String path) => throw UnimplementedError();
